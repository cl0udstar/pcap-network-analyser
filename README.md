# cap-network-analyser
A terminal tool for analysing `.pcap` captures. It parses packets, builds tabular summaries, computes simple statistics and generates visualisations (line chart &amp; communication graph). 
